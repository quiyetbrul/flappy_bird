name: makefile

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install SFML
        run: sudo apt install -y libsfml-dev
      - name: Make
        run: make all
